#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter the numbers: ");
	scanf("%d %d", &a, &b);
	printf("Entered numbers are: %d,%d", a,b);
	//Now to swap numbers//
	a = a+b;
	b = a-b;
	a = a-b;
	
	printf("\nThe Swapped numbers are : %d, %d", a, b);
	return 0;
}
